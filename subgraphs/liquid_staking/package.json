{
  "name": "@airdao/airdao-node-contracts/subgraph",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined airdao/liquid_staking",
    "create-local": "graph create --node http://localhost:8020/ airdao/liquid_staking",
    "remove-local": "graph remove --node http://localhost:8020/ airdao/liquid_staking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 airdao/liquid_staking",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
